# Nonlinear operators, quotient constructions, differentiated formulas and bounds

This directory contains unchanged supporting modules from the accepted proof closure. Start with the [public topic guides](../../README.md); use [MODULES.tsv](../../MODULES.tsv) to locate a particular module. Historical alphabetic prefixes are retained to preserve imports and cached outputs.
